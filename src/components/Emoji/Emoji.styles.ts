import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 8,
    marginRight: 8,
    padding: 8,
  },
  icon: {
    fontSize: 20,
    textAlign: 'center',
  },
});
